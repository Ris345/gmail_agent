services:
  gmail-agent:
    build: .
    ports:
      - "8080:5001"
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./gmail:/app/gmail
